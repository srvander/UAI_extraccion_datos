(()=>{const button=document.querySelector('.menu-button');const nav=document.querySelector('.main-nav');button?.addEventListener('click',()=>{const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');button?.setAttribute('aria-expanded','false')}));const cards=[...document.querySelectorAll('[data-stage]')];const panels=[...document.querySelectorAll('.stage-detail')];function openStage(id,scroll=true){const target=document.getElementById(id);if(!target)return;cards.forEach(c=>{const selected=c.dataset.stage===id;c.classList.toggle('active',selected);c.setAttribute('aria-expanded',String(selected))});panels.forEach(p=>{const selected=p===target;p.classList.toggle('active',selected);p.hidden=!selected});if(scroll)target.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'','#'+id)}cards.forEach(c=>c.addEventListener('click',()=>openStage(c.dataset.stage)));document.querySelectorAll('.close-stage').forEach(c=>c.addEventListener('click',()=>{cards.forEach(x=>{x.classList.remove('active');x.setAttribute('aria-expanded','false')});panels.forEach(p=>{p.classList.remove('active');p.hidden=true});history.replaceState(null,'',location.pathname);document.querySelector('.stage-intro')?.scrollIntoView({behavior:'smooth'})}));const initial=location.hash.slice(1);if(initial&&document.getElementById(initial)?.classList.contains('stage-detail'))openStage(initial,false)})();
